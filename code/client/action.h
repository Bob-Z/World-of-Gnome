#ifndef ACTION_CLIENT_H
#define ACTION_CLIENT_H
#include "../common/common.h"

void update_action_bar(context_t * context, GtkWidget * bar);
void do_action_list(context_t * context, const gchar * action);
#endif

